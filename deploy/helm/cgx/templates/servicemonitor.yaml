{{- if .Values.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "cgx.fullname" . }}
  labels:
    {{- include "cgx.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "cgx.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /api/metrics
      interval: {{ .Values.serviceMonitor.interval }}
{{- end }}
