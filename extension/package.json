{
  "name": "cgx",
  "displayName": "CGX",
  "description": "Open the CGX RAG UI inside a VS Code webview panel.",
  "version": "0.0.1",
  "publisher": "cgx",
  "private": true,
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "cgx.openUI",
        "title": "CGX: Open UI",
        "category": "CGX"
      },
      {
        "command": "cgx.refreshUI",
        "title": "CGX: Reload UI",
        "category": "CGX"
      }
    ],
    "configuration": {
      "title": "CGX",
      "properties": {
        "cgx.ui.url": {
          "type": "string",
          "default": "http://localhost:8765",
          "description": "URL of the running CGX web UI. Start the server with `cgx-ui` or `python app.py` before opening the panel."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
